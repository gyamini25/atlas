# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / extension
node_modules/
extension/dist/
extension/media/webview/

# Env / secrets
.env
*.local

# OS
.DS_Store

# Atlas
*.log
*.vsix

# The demo repo's git history is regenerated by demo/build-demo-git-history.sh,
# so we track its source files but never its (rebuildable) .git directory.
demo/*/.git/
