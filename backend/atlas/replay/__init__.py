"""Decision Replay — the signature cinematic feature.

Reconstructs the evolution of a symbol as an ordered set of narrated steps.
Each step explains WHY the code changed (an incident, a decision, an
optimisation), not merely what changed.
"""
